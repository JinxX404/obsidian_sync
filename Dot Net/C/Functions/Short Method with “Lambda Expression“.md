لما يكون عندي ميثود فيها سطر واحد والسطر ده هو الريتيرن مثلا

ف مش محتاج احطها في البادي بتاع الميثود

واعملها مختصره

```C#
public bool isEven( int val)
        {
            return val % 2 == 0;
        }
```

الاختصار هيكون كده

```C#
public bool isEven( int val) => val % 2 == 0;
```